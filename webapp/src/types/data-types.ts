export type Tparagraph = {
    id?: string,
    title?: string,
    children: React.ReactNode,
}
export type Tlogo = {
    src: string,
    alt: string,
    minWidth:string,
    maxWidth: string,
    link?: string
}